import 'package:flutter/material.dart';

class LulzViewport extends StatelessWidget {
  const LulzViewport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Home')),
    );
  }
}
